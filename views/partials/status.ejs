<%
  const statusLabels = { draft: ['草稿', 'Draft'], published: ['已发布', 'Published'], hidden: ['已隐藏', 'Hidden'], placeholder: ['占位', 'Placeholder'] };
%><span class="status status-<%= status %>"><%= tp(statusLabels[status] || [status, status]) %></span>
